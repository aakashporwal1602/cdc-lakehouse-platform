apiVersion: v1
kind: Secret
metadata:
  name: cdc-secrets
  namespace: cdc-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_WITH_SEALED_SECRET"
  S3_ACCESS_KEY: "REPLACE_WITH_SEALED_SECRET"
  S3_SECRET_KEY: "REPLACE_WITH_SEALED_SECRET"
# In production this is managed by SealedSecrets / External Secrets Operator.
