apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cdc-platform-staging
resources:
  - ../../base
patches:
  - path: replicas.yaml
    target: {kind: Deployment, name: trino}
labels:
  - pairs: {environment: staging}
    includeSelectors: false
