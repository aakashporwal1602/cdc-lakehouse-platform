apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cdc-platform
resources:
  - ../../base
patches:
  - path: replicas.yaml
    target: {kind: Deployment, name: trino}
labels:
  - pairs: {environment: production}
    includeSelectors: false
